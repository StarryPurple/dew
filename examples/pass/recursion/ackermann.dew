// expect: 125
// Ackermann function: stress test for deep recursion with else-if chains.
def rec ack = fn(m: Int, n: Int) -> Int {
  if m == 0 { n + 1 }
  else if n == 0 { ack(m - 1, 1) }
  else { ack(m - 1, ack(m, n - 1)) }
};
def main = fn { ack(3, 4) -> stdout; };
