// expect: 55
// Large: default init combined with recursion — sum 1..10 starting from default.
def acc: Int;
def rec sum = fn(n: Int) -> Int {
  if n == 0 { 0 } else { n + sum(n - 1) }
};
def main = fn { sum(10) -> stdout; };
