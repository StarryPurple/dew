// expect: 84
// Tier: Medium
// Borrow on a compound lvalue: field access.
struct Inner { x: Int }
struct Outer { inner: Int }
def read = fn(&v: Int) -> Int { v * 2 }
def main = fn {
  def outer = Outer { inner: Inner { x: 42 } };
  def result = read(&outer.inner.x);
  result -> stdout;
};
