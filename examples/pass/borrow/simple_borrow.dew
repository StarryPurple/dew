// expect: 84
// Tier: Small
// Borrow parameter: &x creates a borrow binding.
struct Data { value: Int }
def show = fn(&d: Data) -> Int { d.value * 2 }
def main = fn {
  def data = Data { value: 42 };
  show(data) -> stdout;
};
