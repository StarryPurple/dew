// expect: 5
// Tier: Medium
// Feature: while-borrow
// while (&x; cond) with Continue
enum __ControlFlow(T) { __Done(T), __Continue(T) }
def main = fn {
  def x = 0;
  while (&x; x < 5) {
    &x = x + 1;
    __Continue((x,))
  };
  x -> stdout
}
