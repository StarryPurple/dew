// expect: 79
// Tier: Small
// Large: import recursive functions, compose with local code.
import "examples/lib/rec.dew"
import "examples/lib/math.dew"
def main = fn { add(fact(4), sum(10)) -> stdout; };
