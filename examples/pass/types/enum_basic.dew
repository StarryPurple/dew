// expect: 42
// Tier: Medium
// Enum declaration and construction with match.
enum Option { Some(Int), None }
def main = fn {
  def val = Some(42);
  match val {
    Some(n) => n,
    None => 0,
  } -> stdout;
};
