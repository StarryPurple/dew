// expect: 42
// Tier: Medium
// Chained if/else with else-if.
def classify = fn(n: Int) -> Int {
  if n < 10 { n * 10 }
  else if n < 50 { n * 2 }
  else if n < 100 { n }
  else { n / 2 }
};
def main = fn { classify(21) -> stdout; };
