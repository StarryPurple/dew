// expect: 42
// Tier: Small
// Affine value inside a lazy thunk: forced once, then cached.
#[affine]
struct Session { id: Int }
def session = Session { id: 42 };
def main = fn { session.id -> stdout; };
