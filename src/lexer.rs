// Lexer: character stream → token stream.
// Tokenizes Dew source code into a sequence of tokens for the parser.

pub struct Lexer;

impl Lexer {
    pub fn new(_source: &str) -> Self {
        Lexer
    }
}
