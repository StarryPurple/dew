// Dew compiler CLI entry point.
// Dispatches to file mode, eval mode, REPL mode, or LSP based on arguments.

fn main() {
    eprintln!("Dew compiler v0.1.0 — not yet implemented");
    std::process::exit(1);
}
