# 简介

Dew 是一门纯函数式、静态类型语言，具有隐式惰性求值、affine 类型和一等闭包。

```dew
{{#include ../../examples/arithmetic.dew}}
```

## 核心概念

- **不可变性**：所有值不可变，通过 `def` 重新绑定
- **惰性求值**：隐式，由严格性分析驱动
- **Affine 类型**：使用一次的编译时资源管理
- **闭包**：捕获值，非引用
